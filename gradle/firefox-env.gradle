import Version

run {
    if (hasProperty("firefoxBin")) {
        environment("firefox.bin", firefoxBin)
    }
}

if (Version.majorVersion(seleniumVersion) > 2) {
    apply from: "gradle/geckodriver.gradle"
}